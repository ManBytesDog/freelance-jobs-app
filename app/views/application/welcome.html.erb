
<% if session[:id] != nil && session[:identify] == "Poster"%>
        <%poster = Poster.find(session[:id])%>

        <h1> Welcome to BorgLancer <%= poster.username%></h2>
        <p> Here is everything that you can do! </p>
        <p> blach blah blah lol</p>
        <h2>This is when someone is signed in </h2>

<% elsif session[:id] != nil && session[:identify] == "Freelancer" %>
        <%freelancer = Freelancer.find(session[:id])%>
        <h1> Welcome to BorgLancer <%= freelancer.username %>


<%else%>
        <p> Need to Log in</p>
        <%= redirect_to home_page_path %>
<%end%>
